# cell.py knows position, the 4 walls and visited states
# maze.py holds the 2d grid and finds a cell's neighbours
# generator.py creates paths thru the maze and knocks down walls using recursive backtracking